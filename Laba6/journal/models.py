"""Модели данных приложения.

Здесь описывается структура записи журнала, а Django сам создаст таблицу
в SQLite после выполнения миграций.

Журнал изменений:
- DEV-AI01 | 2026-04-11 | Причина: приведение документации в коде к требованиям ЛР6.
- Исходный фрагмент: у методов модели не было расширенного описания назначения.
"""

from django.db import models
from django.urls import reverse


# Набор вариантов приоритета помогает форме и шаблонам показывать понятные значения.
PRIORITY_CHOICES = [
    (1, "Высокий"),
    (2, "Обычный"),
    (3, "Низкий"),
]


# Основная сущность лабораторного мини-сайта: запись журнала.
class JournalEntry(models.Model):
    """Модель одной записи журнала совместной разработки.

    Поля модели описывают заголовок, slug-адрес, основной текст и приоритет.
    Даты создания и изменения заполняются автоматически на уровне ORM.
    """

    # Заголовок записи используется в списке и на странице детали.
    title = models.CharField("Заголовок", max_length=120)
    # Уникальный адрес записи нужен для удобной навигации.
    slug = models.SlugField("Слаг", max_length=140, unique=True)
    # Основной текст записи хранит содержимое заметки.
    content = models.TextField("Содержание")
    # Приоритет помогает визуально отделять важные записи.
    priority = models.PositiveSmallIntegerField("Приоритет", choices=PRIORITY_CHOICES, default=2)
    # Метка даты создания используется для сортировки и отображения истории.
    created_at = models.DateTimeField("Создано", auto_now_add=True)
    # Метка последнего изменения показывает, когда запись редактировали.
    updated_at = models.DateTimeField("Изменено", auto_now=True)

    class Meta:
        # Сортируем записи так, чтобы свежие были первыми.
        ordering = ["-created_at", "title"]
        # Человекочитаемое название модели в интерфейсе Django.
        verbose_name = "запись журнала"
        verbose_name_plural = "записи журнала"

    def __str__(self) -> str:
        """Возвращает короткое строковое имя объекта для интерфейсов Django.

        Возвращает:
            str: заголовок текущей записи журнала.
        """

        # Возвращаем название для отображения в админке и отладке.
        return self.title

    def get_absolute_url(self):
        """Формирует канонический URL для просмотра текущей записи.

        Возвращает:
            str: URL-адрес детальной страницы записи по ее slug.
        """

        # Django использует этот метод для перехода на страницу объекта.
        return reverse("journal:entry_detail", kwargs={"slug": self.slug})
